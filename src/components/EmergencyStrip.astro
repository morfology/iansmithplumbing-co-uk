---
import { copy } from '../config/copy'
---

<aside
  class="border-y-2 px-5 py-4"
  style="background: var(--alert-bg); border-color: var(--alert-border); color: var(--alert-text);"
>
  <div class="mx-auto max-w-3xl">
    <p class="font-semibold">{copy.emergencyStrip}</p>
  </div>
</aside>
