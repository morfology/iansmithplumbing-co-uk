---
// NOTE: this is the one piece of trade-specific copy outside client.ts — the
// stopcock advice only makes sense for a plumber. It is not *client*-specific,
// so the one rule holds, but a non-plumbing client will need this lifted into a
// config field. See the handover note.
---

<aside
  class="border-y-2 px-5 py-4"
  style="background: var(--alert-bg); border-color: var(--alert-border); color: var(--alert-text);"
>
  <div class="mx-auto max-w-3xl">
    <p class="font-semibold">
      Water coming through the ceiling? Turn the stopcock off — usually under the
      kitchen sink — then call. Don't wait for a quote.
    </p>
  </div>
</aside>
